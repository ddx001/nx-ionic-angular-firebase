{
  "name": "lib-common",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "lib-common/src",
  "prefix": "lib-common",
  "tags": [],
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "lib-common/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    }
  }
}
